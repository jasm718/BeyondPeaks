import 'package:flutter/widgets.dart';

class AppSpacing {
  const AppSpacing._();

  static const unit = 8.0;
  static const pagePadding = EdgeInsets.fromLTRB(16, 20, 16, 24);
  static const panelPadding = EdgeInsets.all(16);
  static const panelRadius = 8.0;
}
